export const initialState = {
  betAmount: 0,
  betType: "",
  payout: 0,
  win: false,
  status: false,
  higherOrLower: "",
  user: user,
};

const betReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
